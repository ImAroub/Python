# SeedsDatasetClustering

This project applies K-Means clustering on a wheat seeds dataset using geometric 
measurements like area, perimeter, compactness, length, width, asymmetry, and groove length.
The workflow includes data preprocessing, normalization, clustering into three groups,
and PCA-based 2D visualization to show clear geometric differences among seed types.
